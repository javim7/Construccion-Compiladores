class Main {
   
    a : Int <- 1;
    b : Int <- 5;
    c : Int <- 8;
    l : String;
    f : Int;
    flag : Bool <- true;
    x : String;
    z : String;

    main() : SELF_TYPE {
      {   
          l <- "hola" + "adios";
          f <- a + b * c;
          f <- test(4);
          out_bool(flag);
          return (self);
	    }
    };

    test(a: Int): Int {
        return (a)
    }; 

};


class ClassB {
   test(a: Int): String {
        return("hola")
    }; 
};

(new Main).main()