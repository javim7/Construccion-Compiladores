class Main inherits IO {
    a : Int <- 2;
    b : Int <- 5;
    c : Int <- 8;
    l : String;
    f : Int;
    flag : Bool <- true;
    
    main() : SELF_TYPE {
      {   
          l <- "hola" + "adios";
          f <- flag;
          out_int(a);
          test("hola");
          self;
	    }
    };

    test(a : Int) : Int {
        out_int(2)
    };
};