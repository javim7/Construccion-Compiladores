class Main inherits IO {
    a : Int <- 2;
    b : Int <- 5;
    c : Int <- 8;
    l : String;
    f : Int;
    flag : Bool <- true;

    

    main() : SELF_TYPE {
      {   
          l <- "hola" + "adios";
          f <- a + b * c;
          out_int(a);
          test(1, "a", a);
          self;
	    }
    };

    test(x : Int, y : String, z : Int) : Int {
        out_int(2)
    };

    
    
};


class ClassB {
   test(a: Int): String {
        out_int(a)
    }; 
};