class Main {
   
    a : Int <- 1;
    b : Int <- 5;
    c : Int <- 8;
    l : String;
    f : Int;
    flag : Bool <- true;
    x : String;
    z : String;

    main() : SELF_TYPE {
      {   
          l <- "hola" + "adios";
          f <- a + b * c;
          f <- 4;
          x <- test(1);
          z <- test(1);
          self;
	    }
    };

    test(a: Int): String {
        test(a)
    }; 

};


class ClassB {
   test(a: Int): String {
        test(a)
    }; 
};

(new Main).main()