class Main {
   
    a : Int <- 1;
    b : Int <- 5;
    c : Int <- 8;
    l : String;
    f : Int;
    flag : Bool <- true;

    main() : SELF_TYPE {
      {   
          l <- "hola" + "adios";
          f <- a + b * c;
          f <- flag;
          test(1);
          self;
	    }
    };

    test(a: Int): String {
        out_int(a)
    }; 

};


class ClassB inherits IO{
   test(a: Int): String {
        out_int(a)
    }; 
};

(new Main).main()