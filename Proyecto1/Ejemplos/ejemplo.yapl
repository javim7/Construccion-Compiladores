class Main {
    a : Int <- 2;
    b : Int <- 5;
    c : Int <- 8;
    l : String;
    f : Int;
    flag : Bool <- true;

    

    main() : SELF_TYPE {
      {   
          l <- "hola" + "adios";
          f <- a + b * c;
          f <- flag;
          test(1, "a", a);
          self;
	    }
    };


};


class ClassB inherits IO{
   test(a: Int): String {
        out_int(a)
    }; 
};