class Main {
    num1 : Int <- 2;
    num2 : Int <- 3;
    resultado : Int;

    main() : Int {
      aritmeticos()
    };

    aritmeticos(): Int {
      {
       if num1 < 0
       then out_string("es negativo")
       else
       out_string("es positivo")
       fi;
      }
    }
};