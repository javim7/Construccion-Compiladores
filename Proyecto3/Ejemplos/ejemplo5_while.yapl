class Main {
    num1 : Int <- 3;
    num2 : Int <- 9;
    num3 : Int <- 1;

    main(): SELF_TYPE {
        {    
            while num1 < num2
            loop {
                out_int(num2);
                num2 <- num2 - num3;
            }
            pool;
            out_int(num1);
        }
    };
};