class Main {
   
    a : Int <- 1;
    b : Int <- 2;
    c : Int <- 3;
    d : Int <- 4;
    e : Int;

    main() : Int {
      {
        e <- a + a * (b-c) + (b-c) * d;
        return(e);
      }
    };

};