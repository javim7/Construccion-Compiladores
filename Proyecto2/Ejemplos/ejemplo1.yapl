class Main {
    a : Int <- 1;
    b : Int <- 5;
    c : Int <- 8;

    main() : Int {
      {
        b <- 5 - 6  * 10;
        c <- suma(a,b);
        return(c);
      }
    };

    suma(a: Int, b: Int): Int {
      {
        c <- a + b;
        return(c);
      }
    };

};