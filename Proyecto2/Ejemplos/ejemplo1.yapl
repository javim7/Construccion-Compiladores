class Main {
    a : Int <- 1;
    b : Int <- 2;
    c : Int;
    d : Int <- 1;
    e : Int <- 2;

    main() : Int {
      { 
        c <- 2 * a;
        return(d + e);
      }
    };

    metodoB(a:Int) : Int{
      out_int(a)
    };

};