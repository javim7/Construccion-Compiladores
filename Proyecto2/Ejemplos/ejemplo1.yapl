class Main {
    a : Int <- 1;
    b : Int <- 2;
    c : Int;
    d : Int <- 1;
    e : Int <- 2;

    main() : Int {
      { 
        a <- 2 * 3;
        c <- 2 * 3 - 5;
        metodoB(a, b);
        out_int(2);
        
        if a<b
        then a <- 5000
        else if a = b
        then a <- 3000
        else a <- 1000
        fi fi;

        out_int(a);

        return(c);
      }
    };
  
    metodoB(a:Int, b: Int) : Int{
      d <- a / b
    };

};