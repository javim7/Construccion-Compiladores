class Main {
    a : Int <- 1;
    b : Int <- 2;
    c : Int;
    d : Int <- 1;
    e : Int <- 2;

    main() : Int {
      { 
        a <- 2 * 3;
        c <- 2 * 3 - 5;
        metodoB(a, b);
        out_int(2);
        
        if a<b
        then a <- 1
        else a <- 3
        fi;

        out_int(a);

        return(c);
      }
    };
  
    metodoB(a:Int, b: Int) : Int{
      d <- a / b
    };

};