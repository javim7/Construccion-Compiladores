class Main {
    a : Int <- 2;
    b : Int <- 5;
    c : Int <- 8;
    l : String;
    f : Int;
    flag : Bool <- true;

    

    main() : SELF_TYPE {
      {   
          l <- "hola" + "adios";
          f <- a + b * c;
          test(1, "a", a);
          self;
	    }
    };
    
    test(x : Int, y : String, z : Int): String {
        a
    }; 

};


class ClassB inherits IO {
   test(a: Int): String {
        out_int(a)
    }; 
};